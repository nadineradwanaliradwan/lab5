name: Hello Agile 
on:
  push:
    branches: [dev]          # Runs when you push to 'dev' branch
  pull_request:
    branches: [main]         # Runs when you open a PR targeting 'main'
 
jobs: 
  say_hello: 
    runs-on: ubuntu-latest 
    steps:
      - name: Print Hello 
        run: echo "Hello from GitHub Actions!" 